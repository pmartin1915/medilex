name: Tag-Based Build

on:
  push:
    tags:
      - 'medilex-v*.*.*'
      - 'medilex-v*.*.*-alpha.*'
      - 'medilex-v*.*.*-beta.*'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform: [android, ios]
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      
      - name: Install dependencies
        run: npm ci
      
      - name: Extract version and profile
        id: version
        run: |
          TAG=${GITHUB_REF#refs/tags/medilex-v}
          echo "version=$TAG" >> $GITHUB_OUTPUT
          if [[ $TAG == *"-alpha."* ]]; then
            echo "profile=development" >> $GITHUB_OUTPUT
          elif [[ $TAG == *"-beta."* ]]; then
            echo "profile=preview" >> $GITHUB_OUTPUT
          else
            echo "profile=production" >> $GITHUB_OUTPUT
          fi
      
      - name: Build ${{ matrix.platform }} via EAS
        run: |
          eas build --platform ${{ matrix.platform }} --profile ${{ steps.version.outputs.profile }} --non-interactive --wait
      
      - name: Download build
        run: |
          EXT=${{ matrix.platform == 'android' && 'apk' || 'ipa' }}
          eas build:download --platform ${{ matrix.platform }} --profile ${{ steps.version.outputs.profile }} --output medilex-${{ matrix.platform }}-${{ steps.version.outputs.version }}.$EXT
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v1
        with:
          files: medilex-${{ matrix.platform }}-${{ steps.version.outputs.version }}.${{ matrix.platform == 'android' && 'apk' || 'ipa' }}
          tag_name: ${{ github.ref_name }}
          name: Medilex ${{ steps.version.outputs.version }}
          draft: false
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
      
      - name: Notify build status
        if: always()
        shell: pwsh
        run: |
          cd ../scripts
          $status = if ('${{ job.status }}' -eq 'success') { 'success' } else { 'failure' }
          ./notify-build.ps1 -Status $status -App Medilex -Platform ${{ matrix.platform }} -Profile ${{ steps.version.outputs.profile }} -DownloadUrl "https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
